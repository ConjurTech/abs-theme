<!DOCTYPE html>
<!--[if lt IE 7 ]><html class="ie ie6" lang="en"> <![endif]-->
<!--[if IE 7 ]><html class="ie ie7" lang="en"> <![endif]-->
<!--[if IE 8 ]><html class="ie ie8" lang="en"> <![endif]-->
<!--[if (gte IE 9)|!(IE)]><!--><html lang="en"> <!--<![endif]-->
<head>
  <title>
  {{ page_title }}{% if current_tags %}{% assign meta_tags = current_tags | join: ', ' %} &ndash; {{ 'general.meta.tags' | t: tags: meta_tags }}{% endif %}{% if current_page != 1 %} &ndash; {{ 'general.meta.page' | t: page: current_page }}{% endif %}{% unless page_title contains shop.name %} &ndash; {{ shop.name }}{% endunless %}
  </title>
  {% if page_description %}
  <meta name="description" content="{{ page_description | escape }}" />
  {% endif %}
  <link rel="canonical" href="{{ canonical_url }}" />
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
  {% include 'facebook-open-graph' %}
  {% include 'twitter-cards' %}
  {% if settings.header_font contains "PT Serif" or settings.body_font contains "PT Serif" %}
  <link href='//fonts.googleapis.com/css?family=PT+Serif' rel='stylesheet' type='text/css'>
  {% endif %}
  {% if settings.header_font contains "Source Sans Pro" or settings.body_font contains "Source Sans Pro" %}
  <link href='//fonts.googleapis.com/css?family=Source+Sans+Pro:600' rel='stylesheet' type='text/css'>
  {% endif %}
  {% if settings.header_font contains "Cabin" or settings.body_font contains "Cabin" %}
  <link href='//fonts.googleapis.com/css?family=Cabin:400,700' rel='stylesheet' type='text/css'>
  {% endif %}
  {% if settings.header_font contains "Open Sans Condensed" or settings.body_font contains "Open Sans Condensed" %}
  <link href='//fonts.googleapis.com/css?family=Open+Sans+Condensed:300' rel='stylesheet' type='text/css'>
  {% endif %}
  {% if settings.header_font contains "Karla" or settings.body_font contains "Karla" %}
  <link href='//fonts.googleapis.com/css?family=Karla:400,700' rel='stylesheet' type='text/css'>
  {% endif %}
  {% if settings.header_font contains "Work Sans" or settings.body_font contains "Work Sans" %}
  <link href='//fonts.googleapis.com/css?family=Work+Sans:400,700' rel='stylesheet' type='text/css'>
  {% endif %}
  {% if settings.header_font contains "Roboto" or settings.body_font contains "Roboto" %}
  <link href='//fonts.googleapis.com/css?family=Roboto:400,700' rel='stylesheet' type='text/css'>
  {% endif %}
  {% if settings.header_font contains "Lora" or settings.body_font contains "Lora" %}
  <link href='//fonts.googleapis.com/css?family=Lora:400,700' rel='stylesheet' type='text/css'>
  {% endif %}
  {% if settings.header_font contains "Arimo" or settings.body_font contains "Arimo" %}
  <link href='//fonts.googleapis.com/css?family=Arimo:400,700' rel='stylesheet' type='text/css'>
  {% endif %}
  {{ 'theme.scss.css' | asset_url | stylesheet_tag }}
  {{ 'custom-theme.scss.css' | asset_url | stylesheet_tag }}
  {{ 'modernizr.min.js' | asset_url | script_tag }}
  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.0/jquery.min.js" type="text/javascript"></script>
  {{ 'jquery.fancybox.js' | asset_url | script_tag }}
  {% if template contains "product" %}
  <script src="{{ 'jquery.zoom.min.js' | asset_url }}" type="text/javascript"></script>
  {% endif %}
  <!--[if lt IE 9]>
    <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.2/html5shiv.min.js"></script>
  <![endif]-->
  {% if template contains 'customers' %}
    {{ 'shopify_common.js' | shopify_asset_url | script_tag }}
    {{ 'customer_area.js'  | shopify_asset_url | script_tag }}
  {% endif %}
  <!--[if lt IE 8]>
    {{ 'json2.js' | shopify_asset_url | script_tag }}
  <![endif]-->
  {{ 'option_selection.js' | shopify_asset_url | script_tag }}
  <script> Shopify.money_format = "{{ shop.money_format }}";</script>
  {{ 'jquery.theme.js' | asset_url | script_tag }}
  {{ content_for_header }}
</head>

<body id="{{ page_title | handle }}" class="{% if customer %}customer-logged-in {% endif %}template-{{ template | replace: '.', ' ' | truncatewords: 1, '' | handle }}" >
  <div id="cart-animation">1</div>
  <div id="fb-root"></div>
  <script>(function(d, s, id) {
    var js, fjs = d.getElementsByTagName(s)[0];
    if (d.getElementById(id)) return;
    js = d.createElement(s); js.id = id;
    js.src = "//connect.facebook.net/en_US/all.js#xfbml=1&appId=1743713679100277";
    fjs.parentNode.insertBefore(js, fjs);
  }(document, 'script', 'facebook-jssdk'));</script>
 {% if settings.cart_type == 'drawer' %}
  <!--START DRAWER-->
  <div id="drawer">
    <div class="">
      {% if cart.item_count > 0 %}
      <form action="/cart" method="post" name="cartform">
        {% for item in cart.items %}
        <!-- START ROW-->
        <div class="row {% if forloop.first %}first{% endif %}">
          <div class="nine columns description">
            <img src="{{ item | img_url: 'compact' }}" alt="{{ item.title | escape }}" />
            <div class="info">
              <h3>
                <a href="{{ item.url | within: collections.all }}">
                    {{ item.title }}
                    {% if item.product.variants.size == 1 %}
                    {% unless item.variant.title contains 'Default' %}
                    - {{ item.variant.title }}
                    {% endunless %}
                    {% endif %}
                </a>
              </h3>
              <h4>{{ item.vendor }}</h4>
              <p>{{ item.product.description | strip_html | truncatewords: 20 }}</p>
              <p class="mobile">{{ item.quantity }} x {{ item.price | money }}</p>
            </div>
          </div>
          <div class="two columns price desktop">
            {{ item.price | money }}
          </div>
          <div class="two columns quantity desktop">
            <input type="text" name="updates[]" id="updates_{{ item.id }}" value="{{ item.quantity }}" />
          </div>
          <div class="two columns total desktop">
            {{ item.line_price | money }}
          </div>
          <div class="one column remove desktop">
            <a href="/cart/change?line={{ forloop.index }}&quantity=0" class="removeLine" rel="{{ item.variant.id }}">{{ 'cart.general.remove' | t }}</a>
          </div>
        </div>
        <!-- END ROW-->
        {% endfor %}
        <!-- START ACTIONS -->
        <div class="actions">
          <input type="submit" id="update" name="update" value="{{ 'cart.general.update' | t }}" />
          <input type="submit" value="{{ 'cart.general.check_out' | t }}" />
        </div>
        <!-- END ACTIONS -->
      </form>
      {% else %}
      <div class="sixteen columns description">
        <p class="empty">{{ 'cart.general.empty_cart_drawer_message' | t }}</p>
      </div>
      {% endif %}
    </div>
  </div>
  <!--END DRAWER-->
  {% endif %}
   <!--START HEADER-->
  <header>
    <!--START CONTAINER-->
    <div class="container upper">
      <!--START SIDEBAR-->
      {% if template != "index" %}
      <div class="three columns product-search-wrapper">
        <div id="product-search">
          <form action="/search" method="get" class="search-form" role="search">
            <input type="hidden" name="type" value="product" />
            <input type="submit" id="search-submit" class="icon {% if settings.light_search_icon %}light{% endif %}" />
            {% if template == "search" and search.performed and search.terms != "" %}
            <input type="text" name="q" id="search-field" class="search" value="{{ search.terms | escape }}" placeholder="{{ 'general.search.search_form_submit' | t }}"/>
            {% else %}
            <input class="search" placeholder="{{ 'general.search.search_form_submit' | t }}" name="q" type="text" id="search-field"  />
            {% endif %}
          </form>
          <br class="clear">
        </div>
      </div>
      {% endif %}
      <div class="one columns product-search-wrapper">
        <h1 id="logo">
          <a href="/">
            {% if settings.use_logo %}
            {{ 'logo.png' | asset_url | img_tag: shop.name }}
            {% else %}
            <span class="no-logo">{{ shop.name }}</span>
            {% endif %}
          </a>
        </h1>
      </div>
      <!--END SIDEBAR-->
      <!--START TOP NAV-->
      <div class="thirteen columns offset-by-one desktop" id="nav-link-container">
        <nav>
          <!--{% for link in linklists[settings.secondary_menu].links %}
          <a href="{{ link.url }}">{{ link.title }}</a>
          {% endfor %}-->
          <a href="/#">Home</a>
          <a href="/pages/brands">Brands</a>
          <a href="/pages/new">What's New</a>
          <a href="/pages/bestsellers">Bestsellers</a>
          <a href="/pages/events">Events and Promotions</a>
          <a href="/pages/reviews">Reviews</a>
          <a href="/pages/boutiques">Boutiques</a>
          <a href="/pages/awards">Awards</a>

          {% if shop.customer_accounts_enabled %}
            {% if customer %}
              <a href="/account">{{ 'customer.account.your_account' | t }}</a>
              {{ 'customer.account.log_out' | t | customer_logout_link }}
            {% else %}
              {{ 'customer.account.log_in' | t | customer_login_link }}
              {% if shop.customer_accounts_optional %}
                {{ 'customer.account.create_account' | t | customer_register_link }}
              {% endif %}
            {% endif %}
          {% endif %}
        </nav>
      </div>
      <!--END TOP NAV-->
      <!--START MINICART-->
      <div class="one columns minicart-wrapper">
        <div id="minicart">
          <a href="{% if settings.cart_type == 'drawer' %}#{% else %}/cart{% endif %}" class="{% if settings.cart_type == 'drawer' %}toggle-drawer{% endif %} cart desktop {% if settings.light_cart_icon %}light{% endif %}"></a>
         <!-- {% if settings.cart_type == 'drawer' %} <a href="/cart" class="checkout">{{ 'layout.general.header_checkout_text' | t }}</a> {% endif %} -->
        </div>
      </div>
      <!--END MINI CART-->
    </div>
    <!--END CONTAINER-->

    <!--START CONTAINER-->

    <!--<div class="container lower">
      <div class="sixteen columns">
        <div class="table logo-tagline">
          <div class="table-cell">
            <h1 id="logo">
              <a href="/">
                {% if settings.use_logo %}
                {{ 'logo.png' | asset_url | img_tag: shop.name }}
                {% else %}
                <span class="no-logo">{{ shop.name }}</span>
                {% endif %}
              </a>
            </h1>
          </div>

          <div class="table-cell">
            <div id="tagline" class="desktop">
              <h3>
              {% if settings.show_tagline %}
                {% if settings.tagline != blank %}
                  {{ settings.tagline }}
                {% elsif cart.announcements.size > 0 %}
                  {{ cart.announcements.first }}
                {% else %}
                  {{ 'layout.onboarding.tagline_text' | t }}
                {% endif %}
              {% endif %}
              </h3>
            </div>
          </div>
        </div>
      </div>
    </div>-->

  </header>
    <!--START HEADER-->
  <!--START CONTAINER-->
  <div class="">
  <!--START SIDEBAR-->
  <div id="sidebar" class="three columns" style="display: none;">
    <nav>
      <a href="#" id="toggle-menu" class="mobile"><span>{{ 'layout.general.menu_title' | t }}</span></a>
      <ul id="main-menu" class="accordion">
        <li><a href="/#">Home</a></li>
        <li><a href="/pages/brands">Brands</a></li>
        <li><a href="/pages/new">What's New</a></li>
        <li><a href="/pages/bestsellers">Bestsellers</a></li>
        <li><a href="/pages/events">Events and Promotions</a></li>
        <li><a href="/pages/reviews">Reviews</a></li>
        <li><a href="/pages/boutiques">Boutiques</a></li>
        <li><a href="/pages/awards">Awards</a></li>
        <li><a href="/cart" class="cart mobile {% if settings.light_cart_icon %}light{% endif %} cart-target">{{ 'layout.general.header_cart_title' | t }}<span id="cart-target-mobile" class="count cart-target"> ({{ cart.item_count }})</span></a></li>
        {% if shop.customer_accounts_enabled %}
          {% if customer %}
          <li class="mobile"><a href="/account">{{ 'customer.account.your_account' | t }}</a></li>
          <li class="mobile">{{ 'customer.account.log_out' | t | customer_logout_link }}</li>
          {% else %}
          <li class="mobile">{{ 'customer.account.log_in' | t | customer_login_link }}</li>
          {% if shop.customer_accounts_optional %}
          <li class="mobile">{{ 'customer.account.create_account' | t | customer_register_link }}</li>
          {% endif %}
          {% endif %}
        {% endif %}
      </ul>
    </nav>
  </div>
  <script>
  if ($(window).width() < 768) {
    $("#sidebar").show();
  }
  $( window ).resize(function() {
    if ($(window).width() >= 768) {
      $("#sidebar").hide();
    } else {
      $("#sidebar").show();
    }
  });
  </script>
  <!--END SIDEBAR-->

  <div class="">
    <!--START MAIN-->
    <div id="main" class="twelve columns offset-by-one">
      <div id="page-content">
      {{ content_for_layout }}
      <br class="clear">
      </div>
    </div>
    <!--END MAIN-->
    <br class="clear">
    <!--START FOOTER-->
    {% if template != 'index' %}
    <footer>
      <section class="lower">
        <div class="nine columns">
          <div class="legals">
          {% assign current_year = 'now' | date: '%Y' %}
            {{ 'layout.copyright.copyright_html' | t: year: current_year }}, {{ shop.name | link_to: '/' }}. {{ powered_by_link }}.
          </div>
        </div>
        <!-- <div class="six columns offset-by-one">
          {% if linklists.footer.links.size > 0 %}
          <nav>
            {% for link in linklists.footer.links %}
                  <a href="{{ link.url }}" title="{{ link.title }}">{{ link.title }}</a>
              {% endfor %}
          </nav>
          {% endif %}
        </div> -->

        <div class="six columns offset-by-one">
          <div class="inline-list social-icons">

            {% if settings.facebook_url != blank %}
             <span>
               <a class="icon-fallback-text" href="{{ settings.facebook_url }}" target="_blank" title="{{ shop.name }} on Facebook">
                 <span class="social-link-icon social-link-icon-facebook" aria-hidden="true"></span>
                 <span class="fallback-text">Facebook</span>
               </a>
             </span>
           {% endif %}

           {% if settings.twitter_url != blank %}
             <span>
               <a class="icon-fallback-text" href="{{ settings.twitter_url }}" target="_blank" title="{{ shop.name }} on Twitter">
                 <span class="social-link-icon social-link-icon-twitter" aria-hidden="true"></span>
                 <span class="fallback-text">Twitter</span>
               </a>
             </span>
           {% endif %}

           {% if settings.google_url != blank %}
             <span>
               <a class="icon-fallback-text" href="{{ settings.google_url }}" rel="publisher" target="_blank" title="{{ shop.name }} on Google Plus">
                 <span class="social-link-icon social-link-icon-google" aria-hidden="true"></span>
                 <span class="fallback-text">Google Plus</span>
               </a>
             </span>
           {% endif %}

           {% if settings.instagram_url != blank %}
             <span>
               <a class="icon-fallback-text" href="{{ settings.instagram_url }}" target="_blank" title="{{ shop.name }} on Instagram">
                 <span class="social-link-icon social-link-icon-instagram" aria-hidden="true"></span>
                 <span class="fallback-text">Instagram</span>
               </a>
             </span>
           {% endif %}

           {% if settings.pinterest_url != blank %}
             <span>
               <a class="icon-fallback-text" href="{{ settings.pinterest_url }}" target="_blank" title="{{ shop.name }} on Pinterest">
                 <span class="social-link-icon social-link-icon-pinterest" aria-hidden="true"></span>
                 <span class="fallback-text">Pinterest</span>
               </a>
             </span>
           {% endif %}

           {% if settings.youtube_url != blank %}
             <span>
               <a class="icon-fallback-text" href="{{ settings.youtube_url }}" target="_blank" title="{{ shop.name }} on Youtube">
                 <span class="social-link-icon social-link-icon-youtube" aria-hidden="true"></span>
                 <span class="fallback-text">Youtube</span>
               </a>
             </span>
           {% endif %}

           {% if settings.vimeo_url != blank %}
             <span>
               <a class="icon-fallback-text" href="{{ settings.vimeo_url }}" target="_blank" title="{{ shop.name }} on Youtube">
                 <span class="social-link-icon social-link-icon-vimeo" aria-hidden="true"></span>
                 <span class="fallback-text">Vimeo</span>
               </a>
             </span>
           {% endif %}

           {% if settings.tumblr_url != blank %}
             <span>
               <a class="icon-fallback-text" href="{{ settings.tumblr_url }}" target="_blank" title="{{ shop.name }} on Tumblr">
                 <span class="social-link-icon social-link-icon-tumblr" aria-hidden="true"></span>
                 <span class="fallback-text">Tumblr</span>
               </a>
             </span>
           {% endif %}

           {% if settings.show_rss != blank %}
             <span>
               <a class="icon-fallback-text" href="/blogs/{{ settings.main_blog }}.atom" target="_blank" title="{{ shop.name }} on RSS">
                 <span class="social-link-icon social-link-icon-rss" aria-hidden="true"></span>
                 <span class="fallback-text">RSS</span>
               </a>
             </span>
           {% endif %}

         </div>
       </div>

      </section>
      <br class="clear">
    </footer>
    {% endif %}
    <!--END FOOTER-->
  </div>
  <!-- END CONTAINER-->
</body>

</html>
