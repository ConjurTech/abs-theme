
$light-pink: #f3afc5;
$green: #1caca3;
$blue: #21ace3;
$dark-pink: #d72756;
$nav-links-top-offset: 20px;

body {
  background-color: #eee !important;
  // background-color: #fae6e7 !important;
}

div.container.upper, div#page-content {
  border: none !important;
}

#landing-page-image {
  background-image: url({{'landing-splash.jpg' | asset_url}});
  height: 100vh;
}

// Navbar
#nav-link-container {
  margin-top: $nav-links-top-offset;
  nav {
    a {
      color: $green;
      font-size: 16px;
      margin: 0px !important;
      padding: 0 10px;
      border-right: solid 2px $light-pink;
      &:visited {
        color: $green;
      }
      &:hover {
        color: $green;
      }
    }

    a:first-child  {
      border-left: solid 2px $light-pink;
    }
  }
}

.minicart-wrapper {
  margin-top: $nav-links-top-offset;
  .toggle-drawer.cart  {
    background-repeat: no-repeat;
    background-image: url({{'shopping-bag.png' | asset_url}});
  }
}

// Header
.main-header-wrapper-top {
  display: flex;
  text-align: center;
}
.main-header-wrapper-bottom {
  text-align: center;
}
.main-header-label {
  width: 100%;
  line-height: 40px;
  font-size: 50px;
  font-weight: bold;
  text-transform: uppercase;
  color: $dark-pink;
}
.main-header-inline {
  display: inline;
}
.main-header-subtitle {
  color: $dark-pink;
}
.main-header-subtitle::after {
  border: 1px solid black;
}

#product-search {
  input.icon {
    float: left;
    background: url({{ 'sprite.png' | asset_url }}) -9px -130px no-repeat !important;
    width: 11px;
    height: 11px;
    border: none !important;
    text-indent: -10000px;
    padding: 0;
    margin: 5px 5px 0px 5px;
  }
  input.search {
    float: left;
    width: 130px;
    border: 1px solid $light-pink;
    padding: 5px;
    margin-bottom: 0;
    margin-left: 5px;
    margin-top: -2px;
  }
  .search-wrapper {
    display: flex;
    margin-right: 20px;
  }
}

#page-content {
  overflow: visible !important;
}

// Footer
footer {
  background-image: url({{'bg_footer.png' | asset_url}});
  border: 1px solid $blue;
  border-left-width: 2px;
  border-right-width: 2px;
  h2 {
    margin-top: 10px;
    font-size: 10px;
    font-weight: bold;
    text-transform: uppercase;
  }
  input[type="email"] {
    border: 1px solid $blue;
    border-radius: 0;
    display: inline-block;
  }
  input[type="submit"] {
    border: 1px solid $blue!important;
    background: $blue!important;
    margin-left: -4px;
    height: 27px;
    &:hover {
      background: $blue!important;
    }
  }
  form, input {
    margin-bottom: 0!important;
  }
  .row {
    margin-bottom: 10px;
  }
  .social-icons {
    text-align: right;
    img {
      width: 45px;
    }
  }
}

// Brands
#brands-page {
  ul {
    list-style-type: none;
    li {
      margin: 0px;
    }
  }
}

.brand-image-wrapper {
  padding: 10px;
  background-color: #f4d0da;
}

// Reviews
#reviews-container {
  li {
    display: inline-block;
  }
  #video-container {
    border: 2px solid $light-pink;
    padding: 20px 70px;
    .video-nav {
      content: " ";
      cursor: pointer;
      position: absolute;
      display: inline-block;
      background-size: 25px;
      width: 25px;
      height: 25px;
      top: calc(50% - 25px);
      &.previous {
        left: 15px;
        background-image: url({{ 'left-arrow.png' | asset_url}});
      }
      &.next {
        right: 15px;
        background-image: url({{ 'right-arrow.png' | asset_url}});
      }
    }
  }
  .video-description {
    margin-top: 10px;
    text-align: center;
    font-weight: bold;
  }
  .video-title {
    display: block;
    text-align: center;
    font-size: 22px;
    margin-top: 40px;
    margin-bottom: 20px;
    padding-bottom: 15px;
    border-bottom: 1px solid $light-pink;
  }
  .video-overlay {
    display: inline-block;
  }
}

// Paging
#paging {
  padding: 0 !important;
  .current {
    color: black !important;
  }
  a {
    color: $dark-pink;
    &:hover {
      color: $dark-pink;
      text-decoration: none;
    }
    &:visited {
      color: $dark-pink;
    }
  }
}

// Products
.product-grid-item a {
  color: black;
  &:hover {
    color: black;
    text-decoration: none;
  }
  &:visited {
    color: black;
  }
}

// Collection
.upper-right-row {
  margin-bottom: 0;
}

// Collection sort
.sort-by-inline {
  color: $dark-pink;
  display: inline;
}
